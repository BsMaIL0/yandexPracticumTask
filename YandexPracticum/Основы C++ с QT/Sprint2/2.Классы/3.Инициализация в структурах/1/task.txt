Продолжим развивать геометрическую тему.
Что нужно сделать
Добавьте в структуру Point инициализацию так, чтобы переменная, созданная без инициализации, имела нулевые координаты.
Используйте такой main, чтобы проверить своё решение.

#include <cassert>

...

int main() {
    Point p;
    assert(p.x == 0.);
    assert(p.y == 0.);
} 
Как будет тестироваться код
Тесты проверят, что ваша точка инициализируется правильно.